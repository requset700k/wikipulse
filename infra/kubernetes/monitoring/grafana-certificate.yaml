apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: monitoring
spec:
  secretName: cledyu-ca
  issuerRef:
    name: cledyu-ca-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - grafana.cledyu.local
