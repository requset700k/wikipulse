version: 2
ethernets:
  primary:
    match:
      name: "en*"
    set-name: eth0
    dhcp4: false
    addresses:
      - ${ip}
    routes:
      - to: default
        via: ${gateway}
    nameservers:
      addresses: [${dns_servers}]
