---
# Phase 3b: cert-manager + internal CA bootstrap
# 실행: make cert-manager-up
# 전제: make k8s-bootstrap 완료

- name: cert-manager 설치 및 내부 CA 부트스트랩
  hosts: control_plane[0]
  become: true
  gather_facts: true

  roles:
    - role: cert_manager
